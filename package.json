{
    "name": "luut189.github.io",
    "version": "1.0.0",
    "description": "A Personal Website to practice web development",
    "main": "index.ts",
    "devDependencies": {
        "typescript": "^5.3.3"
    },
    "scripts": {
        "dev": "vite --port 8080",
        "build": "vite build --outDir docs",
        "deploy": "git checkout gh-pages && git pull origin main && npm run build && git add . && git commit -m \"build: latest change\" && git push && git checkout main"
    },
    "keywords": [],
    "author": "Tuong Luu",
    "license": "MIT",
    "dependencies": {
        "prettier": "^3.2.4",
        "vite": "^5.0.10"
    }
}
